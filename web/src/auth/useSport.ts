// web/src/auth/useSport.ts
//
// Re-export of the useSport hook from SportContext for ergonomic
// imports. Callers may import either path; this file exists so the
// hook lookup mirrors auth/useAuth.ts.

export { useSport } from './SportContext'
